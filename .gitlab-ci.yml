image: mcr.microsoft.com/dotnet/core/sdk:6

stages:
    - build
    - test

variables:
    proj: "AdventOfCode"

before_script:
    - "cd $proj"
    - "dotnet restore"

build:
    stage: build
    script:
        - "dotnet build"

test:
    stage: test
    script: 
        - "dotnet test"
